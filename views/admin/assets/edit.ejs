<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>CRUD Operations - Sửa sản phẩm</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <style>
    body {
      background: linear-gradient(135deg, #6e8efb, #a777e3);
      color: #fff;
      min-height: 100vh;
      display: flex;
      align-items: center;
      justify-content: center;
    }
    .card {
      background-color: #fff;
      border-radius: 15px;
      box-shadow: 0 8px 20px rgba(0, 0, 0, 0.2);
    }
    .card-header {
      background-color: #6e8efb;
      color: #fff;
      font-weight: bold;
      border-radius: 15px 15px 0 0;
    }
    .form-control {
      border: 2px solid #6e8efb;
      border-radius: 10px;
    }
    .btn-success {
      background-color: #6e8efb;
      border: none;
      border-radius: 50px;
      padding: 0.5rem 1.5rem;
    }
    .btn-success:hover {
      background-color: #5a7dff;
    }
    h2 {
      color: #fff;
    }
  </style>
</head>
<body>
  <div class="container">
    <div class="row">
      <div class="col-md-6 mx-auto">
        <h2 class="text-center mb-4">Sửa sản phẩm</h2>
        <div class="card">
          <div class="card-header text-center">
            Chỉnh sửa thông tin sản phẩm
          </div>
          <div class="card-body">
            <form action="/update/<%= oldPro.id %>" method="POST">
              <input type="hidden" id="edit-id">
              <div class="mb-3">
                <label for="edit-name" class="form-label">Tên Sản Phẩm</label>
                <input type="text" class="form-control form-control-lg" name="name" value="<%= oldPro.name %>" required>
              </div>
              <div class="mb-3">
                <label for="edit-price" class="form-label">Giá</label>
                <input type="number" class="form-control form-control-lg" name="price" value="<%= oldPro.price %>" required>
              </div>
              <div class="mb-3">
                <label for="edit-image" class="form-label">Ảnh</label>
                <input type="url" class="form-control form-control-lg" name="image" value="<%= oldPro.image %>" required>
              </div>
              <div class="d-flex justify-content-between">
                <button type="submit" class="btn btn-success btn-lg">Cập nhật</button>
                <a class="btn btn-secondary btn-lg" href="/list">Quay lại</a>
              </div>
            </form>
          </div>
        </div>
      </div>
    </div>
  </div>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
